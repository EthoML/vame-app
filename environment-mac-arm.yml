name: vame-desktop
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pytorch=2.5.1
  - pip
  - pip:
      - -r requirements.txt

prefix: /opt/anaconda3/envs/vame-desktop
