# install: conda env create -f environment-mac-arm.yml
name: vame-app
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pytorch=2.11.0
  - pip
  - pip:
      - vame-app

prefix: /opt/anaconda3/envs/vame-app
