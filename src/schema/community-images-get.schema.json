{
    "title": "Get Community Images",
    "type": "object",
    "properties": {
        "segmentation_algorithm": {
            "type": "string",
            "enum": [
                "hmm",
                "kmeans"
            ]
        }
    },
    "required": [
        "segmentation_algorithm"
    ]
}