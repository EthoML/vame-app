{
    "title": "Create VAME Project",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "title": "Project Name",
            "description": "Name of the project. No spaces allowed.",
            "allow-spaces": false
        },
        "source_software": {
            "type": "string",
            "title": "Source Software",
            "description": "Pose estimation software used to generate the data",
            "enum": [
                "DeepLabCut",
                "SLEAP",
                "LightningPose"
            ]
        },
        "pes_paths": {
            "type": "string",
            "format": "file",
            "description": "Pose estimation data files. Supported formats: CSV, SLP, H5.",
            "title": "Pose Estimation Files",
            "accept": [
                ".csv",
                ".slp",
                ".h5"
            ],
            "multiple": true
        },
        "videos": {
            "title": "Video Files (Optional)",
            "type": "string",
            "description": "Labeled videos corresponding to the pose estimation files",
            "format": "file",
            "accept": "video/*",
            "multiple": true
        }
    },
    "required": [
        "name",
        "source_software",
        "pes_paths"
    ]
}