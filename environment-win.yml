name: vame-app
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - cudatoolkit=11.8
  - pytorch=2.5.1
  - pip
  - pip:
    - -e .

prefix: /opt/anaconda3/envs/vame-app