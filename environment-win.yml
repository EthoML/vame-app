# install: conda env create -f environment-win.yml
name: vame-app
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - cudatoolkit=11.8
  - pytorch=2.11.0
  - pip
  - pip:
    - vame-app

prefix: /opt/anaconda3/envs/vame-app