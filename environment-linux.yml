# install: conda env create -f environment-linux.yml
name: vame-app
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
    - vame-app

prefix: /opt/anaconda3/envs/vame-app